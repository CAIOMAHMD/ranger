nodes:
  - address: ec2-52-49-113-31.eu-west-1.compute.amazonaws.com
    user: ubuntu
    role: [controlplane, worker, etcd]
  - address: ec2-3-249-223-186.eu-west-1.compute.amazonaws.com
    user: ubuntu
    role: [controlplane, worker, etcd]
  - address: ec2-46-137-5-51.eu-west-1.compute.amazonaws.com
    user: ubuntu
    role: [controlplane, worker, etcd]

services:
  etcd:
    snapshot: true
    creation: 6h
    retention: 24h

# Required for external TLS termination with
# ingress-nginx v0.22+
ingress:
  provider: nginx
  options:
    use-forwarded-headers: "true"